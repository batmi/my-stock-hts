# ============================================================================
# 개발·테스트 전용 의존성
# ============================================================================
# 운영 기동(run.sh)은 이 파일을 읽지 않는다 — 매매 시스템이 뜨는 데 테스트 도구가
#  필요하지 않고, 라즈베리파이 기동 경로를 그만큼 짧게 두려는 것이다.
#   개발기:  pip install -r requirements-dev.txt
# ============================================================================
-r requirements.txt

pytest>=8.0
pytest-xdist>=3.5      # 병렬 실행(-n). 일부 시간 의존 테스트는 플래키할 수 있다.
pytest-cov>=5.0        # 커버리지 측정(선택)
