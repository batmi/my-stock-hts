# MyStock HTS (Home Trading System)

[![License](https://img.shields.io/badge/License-Apache_2.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)

한국투자증권(KIS) Open API를 활용한 파이썬 기반의 개인용 주식 자동매매 및 분석 시스템입니다.
터미널(Console) 환경에서 동작하며, 실시간 시세 조회, 정밀한 기술적 분석, 그리고 전략 기반의 자동매매 기능을 제공합니다.

## ⚠️ 면책 조항 (Disclaimer)

> **[중요] 본 소프트웨어를 사용하기 전에 반드시 읽어주세요.**

1. **투자 결과에 대한 책임**: 본 소프트웨어는 개발 및 학습 목적으로 작성되었으며, 투자의 보조 수단일 뿐입니다. 본 소프트웨어를 사용하여 발생한 모든 투자 손실 및 피해에 대한 책임은 전적으로 **사용자 본인**에게 있으며, 개발자는 이에 대해 어떠한 법적, 금전적 책임도 지지 않습니다.
2. **시스템 리스크**: API 서버의 장애, 네트워크 불안정, 프로그램의 버그, 로직 오류 등으로 인해 주문이 누락되거나, 중복 실행되거나, 의도치 않은 가격에 체결될 수 있습니다. 사용자는 시스템을 지속적으로 모니터링해야 하며, 비상 상황 시 즉시 프로그램을 중단하고 증권사 HTS/MTS를 통해 직접 대응해야 합니다.
3. **검증 권장**: 실전 투자에 앞서 반드시 **모의투자** 환경에서 충분한 기간 동안 시스템의 안정성과 전략의 유효성을 검증하시기 바랍니다.

---

## 1. 프로그램 개요 및 목적
이 프로그램은 **기술적 분석을 중시하는 퀀트(Quant) 및 시스템 트레이더**를 위해 설계된 CLI(Command Line Interface) 기반의 올인원 주식 도구입니다.
무거운 HTS(홈 트레이딩 시스템) 없이도 터미널 환경에서 빠르고 직관적으로 시세 조회, 잔고 확인, 주문 실행을 수행할 수 있으며, 감에 의존하기보다 **데이터와 지표(EMA 정배열, RSI, MACD 등)**에 근거하여 매매 타이밍을 잡을 수 있도록 돕습니다.

### 주요 특징
*   **HTS/MTS 대체:** 터미널에서 실시간 시세 조회 및 매수/매도/정정/취소 주문 실행
*   **기술적 분석 자동화:** 복잡한 보조지표 계산을 자동화하여 **'매수/상승/관망/주의/매도'** 등의 직관적인 투자 판단 신호 제공
*   **개별 종목 전략 설정:** 종목별로 다른 매수/매도 기준(점수, RSI)과 익절/손절/트레일링 스탑 비율을 개별적으로 설정 가능
*   **시장 지수 필터링:** KOSPI/KOSDAQ 지수의 추세를 분석하여 하락장에서는 자동으로 매수를 보류하는 리스크 관리 기능
*   **실시간 설정 변경:** 시스템 트레이딩 실행 중 매수/매도 조건 및 투자 비중을 즉시 변경하고 영구 반영하는 기능
*   **트레이딩 제한 종목 관리:** 특정 종목을 시스템 트레이딩 대상에서 제외(매수/매도 제한)하고, 매수 시그널 발생 시 알림만 받도록 설정 가능
*   **국내/해외 통합 관리:** 한국투자증권(KIS) API를 활용하여 국내 주식과 미국 주식을 하나의 인터페이스에서 통합 관리
*   **실전/모의 투자 지원:** 설정 변경만으로 모의 투자와 실전 투자를 손쉽게 전환
*   **전략 백테스팅:** 과거 데이터를 기반으로 현재의 매매 전략을 검증하고 수익률을 시뮬레이션
*   **API 통신 최적화:** KIS API 초당 호출 한도(실전 20 TPS, 모의 2 TPS)에 맞춰 멀티스레드와 순차 처리를 자동 전환하여 통신 병목 최소화
*   **스레드 안전(Thread-Safe) DB:** 작업 큐(Queue) 기반의 DB 프록시 아키텍처를 도입하여 다중 스레드 환경에서 발생하는 SQLite Lock 문제 원천 차단

## 2. 필수 준비 사항 (Prerequisites)
이 프로그램은 **한국투자증권(Korea Investment & Securities)**의 Open API를 기반으로 동작합니다.
따라서 프로그램을 정상적으로 실행하기 위해서는 반드시 한국투자증권의 계좌와 API 접근 키가 필요합니다.

### 준비 절차 (Step-by-Step)
1.  **한국투자증권 계좌 개설**: 스마트폰 앱('한국투자증권') 등을 통해 비대면 계좌 개설
2.  **KIS Developers 서비스 신청**: [한국투자증권 KIS Developers](https://apiportal.koreainvestment.com/) 접속 및 서비스 신청
3.  **모의투자 신청 (권장)**: 홈페이지/HTS에서 상시 모의투자 신청 (테스트용)
4.  **API Key 발급**: 마이페이지 > 내 서비스 > 발급받기 (실전/모의 각각 발급 가능)
5.  **환경 변수 등록**: 발급받은 Key와 계좌번호를 시스템 환경 변수로 등록 (아래 설치 방법 참고)

## 3. 설치 및 설정

### 1. 환경 설정
*   Python 3.9 이상

### 2. 라이브러리 설치
**[권장]** `run.sh` (macOS/Linux) 또는 `run.bat` (Windows) 스크립트를 실행하면 필요한 라이브러리를 확인하고 **자동으로 설치를 안내**합니다.

수동으로 설치하려면 아래 명령어를 사용하세요.
```bash
pip install requests pandas yfinance rich matplotlib numpy openpyxl
```

### 3. 설정 (Configuration)
보안을 위해 API Key 등 민감 정보는 **환경 변수**로 설정하는 것을 권장합니다.

**주요 환경 변수:**
*   `SIM_APP_KEY`, `SIM_APP_SECRET`: 모의투자 API Key
*   `REAL_APP_KEY`, `REAL_APP_SECRET`: 실전투자 API Key
*   `AUTO_APP_KEY`, `AUTO_APP_SECRET`: (선택) 자동매매 전용 계좌 API Key (실전투자 시 분리 운용 가능)
*   `SIM_ACC_NUM`: 모의투자 계좌번호 (예: 12345678-01)
*   `REAL_ACC_NUM`: 실전투자 계좌번호
*   `AUTO_ACC_NUM`: (선택) 자동매매 전용 계좌번호
*   `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`: 텔레그램 알림 설정
*   `GEMINI_API_KEY`: (선택) 테마 분석용 Google Gemini API Key (발급받기)
*   `GEMINI_MODEL`: (선택) 사용할 Gemini 모델명 (기본값: `gemini-3.1-flash-lite-preview`)

*또는 `config.py` 파일을 직접 수정하여 기본 설정을 변경할 수 있습니다.*

### 4. 실행
```bash
# 실행 권한 부여 (최초 1회)
chmod +x run.sh

# 메인 프로그램 실행 (대화형 메뉴)
./run.sh

# 도움말 및 옵션 확인
./run.sh --help

# 자동매매 모드로 바로 시작 (예: 모의투자)
./run.sh --mode 1 --auto
```

## 📱 텔레그램 알림 및 제어 설정 (Telegram Bot)

시스템 트레이딩의 매매 내역 알림을 받고 원격으로 제어하기 위해 텔레그램 봇 연동을 권장합니다.

### 1. 봇 생성 (BotFather)
1. 텔레그램에서 **@BotFather** 검색 후 대화 시작.
2. `/newbot` 입력 후 안내에 따라 봇 이름과 ID 설정.
3. 발급된 **API Token** 복사.

### 2. Chat ID 확인
1. 생성한 봇에게 아무 메시지나 전송 (예: "start").
2. 프로젝트 폴더의 `get_telegram_chat_id.py` 실행:
   ```bash
   python tools/get_telegram_chat_id.py
   ```
3. 토큰을 입력하면 **Chat ID**가 출력됩니다.

### 3. 환경 변수 등록
`config.py` 또는 시스템 환경 변수에 설정합니다.
*   `TELEGRAM_BOT_TOKEN`: API Token
*   `TELEGRAM_CHAT_ID`: Chat ID

### 4. 주요 명령어
*   **상태 및 자산**: `/status` (상태), `/balance` (잔고), `/holdings` (보유종목), `/profit [d/w/m]` (기간별 손익)
*   **제어**: `/start` (시작), `/stop` (중단), `/restart` (재시작)
*   **분석**: `/market` (시장지수), `/signal <종목>` (종목진단), `/chart <종목>` (차트전송)
*   **정보**: `/report` (성과리포트), `/history [건수]` (체결내역), `/log [줄수]` (시스템로그)
*   **설정**: `/config` (전략설정), `/rules [종목]` (개별룰), `/stocks` (감시종목)


## 📊 매매 전략 개요 (Trading Strategy)

본 시스템은 **추세 추종(Trend Following)** 전략을 기반으로 하며, 다각적인 기술적 분석을 통해 확률 높은 진입 시점을 포착하고 철저한 리스크 관리를 수행합니다.

### 1. 매수 전략 (Buy Strategy)
매수는 **퀀트 멀티팩터 모델(Quant Multi-Factor Model)**을 통해 산출된 종합 점수와 필터링 조건을 모두 만족할 때 실행됩니다.

*   **진입 조건 (AND 조건)**:
    1.  **종합 점수**: **7.0점 이상** (10점 만점)
    2.  **과열 방지**: RSI가 **65 미만** (상승 여력이 남아있는 구간)
    3.  **수급 확인**: 체결강도 **100% 이상** (매수세 우위)
    4.  **시장 필터**: KOSPI/KOSDAQ 지수가 20일 이평선 위에 위치 (하락장 회피)

*   **스코어링 모델 (총 10점)**:
    *   **추세 (Trend)**: 이동평균 정배열, MACD 골든크로스, 파라볼릭 SAR 상승
    *   **모멘텀 (Momentum)**: RSI 강세 구간(50~75), CCI 상승
    *   **강도 (Strength)**: ADX 추세 강도(20 이상), OBV 수급 상승
    *   **가산점 (Bonus)**: 지표 간 동조화(Synergy) 발생 시 추가 점수 부여

*   **하락장 전용: 낙폭과대 역추세 매수 (Mean Reversion)**:
    *   대세 하락장이나 개별 악재로 인한 급락 시, 일시적인 과매도(침체) 구간에서의 기술적 반등을 노리는 전략입니다.
    *   종합 점수가 미달하더라도 다음 조건을 모두 만족하면 **'역추세매수(🟣)'**로 진입합니다.
        *   **조건 1**: 20일선 대비 이격도가 매우 낮음 (기본 `90%` 이하)
        *   **조건 2**: RSI가 과매도 도달 후 상승세로 전환 (기본 `40` 이하 & 전일 대비 상승)
        *   **조건 3**: 바닥권 매수세(스마트머니) 유입 확인 (체결강도 `120%` 이상)

### 2. 매도 전략 (Sell Strategy)
수익을 보전하고 손실을 제한하기 위해 다음의 매도 로직이 실시간으로 감시됩니다. (우선순위 적용)

1.  **익절 (Take Profit)**: 수익률이 목표치(**+30.0%**)에 도달하면 즉시 이익 실현.
2.  **반익절 (Half Take-Profit)**: 잦은 변동성 장세 대응을 위해, 목표 익절 수익률의 절반에 도달하면 보유 수량의 **50%를 1차로 선매도**하여 수익을 조기 확정하고 승률을 방어합니다.
3.  **손절 (Stop Loss)**: 손실률이 한계치(**-7.0%**)에 도달하면 즉시 매도하여 손실 확정.
4.  **시간 청산 (Time-based Stop)**: 횡보 종목의 기회비용을 줄이기 위해, 매수 후 설정된 기간(기본 10일) 내에 목표 최소 수익(예: +3.0%)에 도달하지 못하면 강제 청산합니다.
5.  **트레일링 스탑 (Trailing Stop)**: 수익률이 **+15.0%** 이상 도달한 후, 고점 대비 **-5.0%** 하락하면 이익 보전 매도.
6.  **과열 매도 (RSI)**: RSI가 **75**를 초과하면 과매수 상태로 판단하여 선제적 매도.
7.  **추세 이탈 (Trend Broken)**: 종합 점수가 **5.0점 미만**으로 하락하거나 주요 지지선(60일/120일선)이 붕괴되면 매도.
8.  **ATR 동적 손절**: `USE_ATR_STOP` 설정 시, 매수 시점의 변동성(ATR)을 기반으로 개별 종목마다 다른 손절률을 적용합니다. (예: ATR * 2.0)

### 스코어링 기준 (Scoring System)
매수 여부를 판단하는 종합 점수는 **퀀트 멀티팩터 모델**에 기반하여 계산됩니다. (총점 10점 만점, 0.5점 단위)
*각 팩터의 배점은 설정(`SCORING_WEIGHTS`)을 통해 조절 가능하며, 아래는 기본값 기준입니다.*

1.  **Trend Factor (추세 팩터) [기본 4.0점]**
    *   **이동평균**: 현재가 > 20일선(+0.5), 20일선 > 60일선(+0.5), 60일선 > 120일선(+0.5)
    *   **MACD**: MACD > Signal (골든크로스 +1.0), MACD > 0 (상승국면 +0.5)
    *   **SAR**: 주가 > SAR (상승 추세 +1.0)

2.  **Momentum Factor (모멘텀 팩터) [기본 2.5점]**
    *   **RSI**: 50 ≤ RSI ≤ 75 (강세 구간 +1.5), 30 ≤ RSI < 50 (반등 시도 +0.5)
    *   **CCI**: CCI > 0 (상승 추세 +0.5), CCI > 100 (강한 탄력 +0.5)

3.  **Strength & Volume Factor (강도 및 수급 팩터) [기본 1.5점]**
    *   **ADX**: ADX ≥ 20 (추세 형성 +0.5)
    *   **OBV**: OBV > OBV 이동평균 (수급 양호 +1.0)

4.  **Synergy Bonus (상관관계 가산점) [기본 2.0점]**
    *   **추세 확증**: (20일선 > 60일선) AND (MACD > 0) AND (ADX ≥ 20) (+1.0)
    *   **모멘텀 폭발**: (MACD > Signal) AND (RSI ≥ 50) AND (OBV 상승) (+1.0)

#### 점수대별 판단 기준 (Scoring Guide)
*   **8.5점 ~ 10.0점 (Strong Buy)**: **강력 매수**. 모든 지표가 상승을 가리키며 상관관계까지 완벽한 상태입니다. 비중을 실어 진입할 수 있습니다.
*   **7.0점 ~ 8.0점 (Buy)**: **매수**. 추세는 확실하나 일부 보조지표가 아직 따라오지 못한 상태입니다. 분할 매수로 진입하기 좋습니다.
*   **5.5점 ~ 6.5점 (Watch)**: **관망/준비**. 상승 초입이거나 추세가 약화되는 중입니다. 섣불리 진입하기보다 7점대로 올라서는지 지켜봐야 합니다.
*   **5.0점 미만 (Sell/Avoid)**: **매도/진입 금지**. 하락 추세이거나 뚜렷한 방향성이 없는 횡보장입니다.

## ⚙️ 전략 파라미터 상세 (Configuration)

`config.py` 파일에서 **기본 전략(Global Strategy)**을 설정할 수 있으며, 프로그램 내 **'시스템 트레이딩 > 종목별 트레이딩 룰'** 메뉴를 통해 종목별로 개별 설정을 적용할 수 있습니다.
또한, **'메인 메뉴 > [0] 시스템 설정'** 메뉴를 통해 실행 중에도 전역 설정을 실시간으로 수정할 수 있습니다. (재시작 시에도 유지됨)

### 1. 기술적 지표 설정 (`INDICATOR_PARAMS`)
*   **데이터 조회 기간 (`CHART_LOOKBACK_DAYS`)**: 730일 (2년). 이동평균선(120일선 등)의 정확한 계산을 위해 충분한 과거 데이터를 조회합니다.
*   **RSI (상대강도지수)**:
    *   기간: 14일 (`RSI_PERIOD`)
    *   과매수 기준: 70 (`RSI_UPPER`) / 과매도 기준: 30 (`RSI_LOWER`)
*   **Parabolic SAR (추세 반전)**:
    *   가속변수(AF): 시작 0.02 (`SAR_AF_START`), 증가 0.02 (`SAR_AF_STEP`), 최대 0.2 (`SAR_AF_MAX`)
    *   값을 키우면 추세 반전 신호가 빨라지지만 속임수 신호가 늘어날 수 있습니다.
*   **CCI (상품채널지수)**:
    *   기간: 20일 (`CCI_WINDOW`)
    *   과매수/과매도 기준: ±100 (`CCI_UPPER`, `CCI_LOWER`)
*   **ADX (추세 강도)**: 기간 14일 (`ADX_PERIOD`). 25 이상일 때 강한 추세로 간주합니다.

### 2. 매수/분석 임계값 (`ANALYSIS_THRESHOLDS`)
-   **매수 기준 점수 (`BUY_SCORE`)**: 기본값 **7.0점**. 여러 기술적 지표(이평선, SAR, MACD, RSI, ADX 등)를 종합한 점수가 이 값 이상일 때 매수 신호가 발생합니다.
*   **상승 추세 점수 (`RISE_SCORE`)**: 기본값 **6점**. 매수 기준에는 미치지 못하지만 상승 흐름이 있는 상태입니다.
*   **매수 허용 RSI 상한 (`BUY_RSI_MAX`)**: 기본값 **65**. 매수 점수를 충족하더라도, RSI가 이 값 이상이면 이미 과열된 것으로 보고 진입하지 않습니다.
*   **매수 체결강도 기준 (`BUY_VOL_STRENGTH`)**: 기본값 **100.0%**. 매수 시점의 체결강도가 이 값 이상이어야 진입합니다. (매수세 우위 확인)
*   **역추세 매수 (`USE_MEAN_REVERSION`)**: 하락장이나 급락 구간에서 지표가 과매도에 도달한 후 반등하는 시점을 포착합니다.
    *   `MR_RSI_MAX`: 역추세 진입 허용 최대 RSI (기본 40.0)
    *   `MR_DISPARITY_MAX`: 20일선 대비 이격도 제한 (기본 90.0% 이하)
    *   `MR_VOL_STRENGTH`: 바닥권 매수세 확인을 위한 높은 체결강도 (기본 120.0%)

### 3. 매도 전략 (`SELL_STRATEGY`)
*   **익절 (Take Profit)**: 수익률 **+30.0%** (`TAKE_PROFIT_RATE`) 도달 시 이익 실현.
*   **반익절 (Half Take-Profit)**: `HALF_TAKE_PROFIT_USE`가 True일 경우, 익절 목표치의 절반 도달 시 보유 물량의 50%를 선매도하여 수익을 조기 확정합니다.
*   **손절 (Stop Loss)**: 손실률 **-7.0%** (`STOP_LOSS_RATE`) 도달 시 손실 확정.
*   **시간 청산 (Time-based Stop)**: `TIME_STOP_USE`가 True일 경우, 매수 후 설정된 일수(`TIME_STOP_DAYS`, 기본 10일) 내에 목표 수익(`TIME_STOP_MIN_PROFIT_RATE`, 기본 3.0%)에 미달하면 기회비용 확보를 위해 매도합니다.
*   **트레일링 스탑 (Trailing Stop)**:
    *   **발동 조건**: 수익률 **+15.0%** (`TRAILING_STOP_ACTIVATION_RATE`) 도달 시 감시 시작.
    *   **매도 조건**: 최고가 대비 **-5.0%** (`TRAILING_STOP_CALLBACK_RATE`) 하락 시 매도.
*   **과열 매도**: RSI가 **75** (`TAKE_PROFIT_RSI`)를 초과하면 과매수 구간으로 판단하여 선제적 매도.
*   **추세 이탈 매도**: 종합 점수가 **5점** (`SELL_SCORE`) 미만으로 떨어지면 매도.
*   **ATR 손절**: `USE_ATR_STOP`이 True일 경우, 고정 손절률 대신 매수 시점의 ATR * `ATR_STOP_MULTIPLIER` 만큼의 비율을 손절률로 사용합니다.

### 4. 리스크 관리 및 필터링 (Risk Management)
*   **슬리피지(Slippage) 보정**:
    *   **개요**: 주문 시점의 가격과 실제 체결 가격의 차이(불리한 체결)를 보정하기 위한 설정입니다.
    *   **백테스팅**: 매수 시 현재가보다 비싸게, 매도 시 싸게 체결된 것으로 가정하여 실제 매매 비용(호가 차이, 체결 지연 등)을 보수적으로 반영합니다.
    *   **실전/자동매매**: 시장가 주문(해외주식의 경우 현재가 기준 지정가) 시, 현재가보다 유리한 방향(매수: +0.1%, 매도: -0.1%)으로 주문 가격을 보정하여 급변하는 시세 속에서도 체결 확률을 높입니다.
    *   **설정값**: 기본값 `0.002` (0.2%). `config.py` 또는 설정 메뉴에서 변경 가능합니다.
    *   **추천 설정**:
        *   **대형주/ETF**: 0.1% ~ 0.2% (유동성이 풍부하여 호가 공백이 적음)
        *   **일반/코스닥**: 0.3% ~ 0.5% (일반적인 시스템 트레이딩 권장값)
        *   **급등주/변동성**: 0.5% ~ 1.0% (체결을 최우선으로 할 때)
        
*   **Monte Carlo 시뮬레이션**:
    *   **개요**: 전략의 견고성(Robustness)을 검증하기 위해 노이즈를 추가하여 반복 테스트하는 기능입니다.
    *   **방법**: 가격 데이터에 ±1%의 랜덤 노이즈를 섞고, 슬리피지 변동 및 체결 누락(1% 확률) 등의 불확실성을 반영하여 1000회 반복 실행합니다.
    *   **결과**: 평균 수익률, 최악의 경우(VaR 95%), 수익률 표준편차 등을 통해 전략이 운에 의한 것인지 실력인지 검증할 수 있습니다.

*   **시장 지수 필터링**: KOSPI/KOSDAQ 지수가 이동평균선(기본 20일) 아래로 하락하면 '하락장'으로 간주하여 신규 매수를 자동으로 보류합니다.
*   **기술적 필터링**: MACD 데드크로스, 주요 이평선(60일/120일) 이탈, SAR 매도 신호 등 추세 훼손 신호가 발생하면 '주의' 상태로 분류하여 **신규 매수를 보류**합니다. (보유 종목은 매도 기준 도달 시까지 유지)
*   **일일 손실 제한**: 당일 추정 자산이 설정된 손실 한도(예: -10%)를 초과하여 하락하면 시스템을 비상 정지합니다.
*   **최대 보유 종목 수**: 포트폴리오에 담을 수 있는 최대 종목 개수를 제한합니다. (기본 5종목)
*   **일일 최대 매매 횟수 제한**: 시스템 폭주나 과도한 매매를 방지하기 위해 하루 최대 매매 횟수를 제한합니다. (기본 50회)
*   **연속 오류 방지 (Kill Switch)**: API 호출이나 시스템 오류가 연속으로 발생(기본 5회)하면 시스템을 대기 모드로 전환하여 계좌를 보호합니다.
*   **주문 상태 머신 (Order State Machine)**: 주문 접수부터 체결까지의 상태를 엄격하게 관리하여 중복 매수나 유령 포지션을 방지합니다.
*   **리스크 기반 포지션 사이징**: 1회 매매 시 계좌 대비 최대 손실폭(기본 5%)을 제한하여, 손절폭이 큰 종목은 자동으로 매수 비중을 줄입니다.
*   **변동성 타겟팅 (Volatility Targeting)**: ATR 지표를 활용해 시장 변동성이 클 때는 현금 비중을 늘리고, 작을 때는 투자를 확대하여 계좌의 변동성을 일정하게 유지합니다. (마지막 종목 매수 시에도 리스크 한도를 준수하여 비중을 조절합니다)

### 5. 스코어링 가중치 최적화 (Scoring Weights)
*   **개요**: 매수 점수 산출 시 각 팩터(추세, 모멘텀, 강도, 시너지)가 차지하는 비중을 사용자가 직접 설정하거나 최적화할 수 있습니다.
*   **설정 항목**:
    *   `TREND`: 추세 팩터 가중치 (기본 4.0)
    *   `MOMENTUM`: 모멘텀 팩터 가중치 (기본 2.5)
    *   `STRENGTH`: 강도/수급 팩터 가중치 (기본 1.5)
    *   `SYNERGY`: 시너지 가산점 (기본 2.0)
*   **백테스팅 최적화**: 백테스팅 메뉴의 '가중치 최적화' 기능을 통해 과거 데이터 기반으로 가장 수익률이 높은 가중치 조합을 찾을 수 있습니다.

### 6. 적응형 임계값 (Adaptive Thresholds)
*   **개요**: 시장의 국면(강세/약세/횡보)을 실시간으로 분석하여 매수 기준 점수(`BUY_SCORE`)를 동적으로 조절합니다.
*   **작동 방식**:
    *   **강세장 (Bull)**: 지수가 이평선 위에 있고 상승 추세일 때, 매수 기준을 낮춰(예: -1.0점) 적극적으로 진입합니다.
    *   **약세장 (Bear)**: 지수가 이평선 아래에 있을 때, 매수 기준을 높여(예: +1.0점) 보수적으로 접근합니다.
    *   **횡보장 (Sideways)**: 뚜렷한 추세가 없을 때, 기본 기준을 유지합니다.
*   **설정**: `config.py` 또는 설정 메뉴에서 보정값(`BULL_SCORE_ADJ`, `BEAR_SCORE_ADJ` 등)을 변경할 수 있습니다.

### 7. 테마 트랜드 분석
*   **개요**: 네이버 금융 데이터 크롤링과 Google Gemini(LLM)를 결합하여 실시간 테마 순위와 시장 분석 정보를 제공합니다.
*   **기능**:
    *   **네이버 테마 순위**: 실시간 등락률 상위 테마와 주도주 정보를 크롤링하여 제공합니다.
    *   **AI 시장 분석**: Gemini의 Google Search Grounding을 활용해 현재 시장의 주도 테마와 상승 원인을 분석합니다.
    *   **커스텀 분석**: 사용자가 직접 프롬프트를 입력하여 특정 이슈나 섹터에 대해 AI에게 질문할 수 있습니다.
*   **설정**: AI 분석 기능을 사용하려면 `GEMINI_API_KEY` 설정이 필요합니다.

### 8. 모의투자 환경 유의사항 (Known Issues)
*   **미체결 내역 조회 오류**: 한국투자증권 모의투자 시스템의 API 버그로 인해, 주문이 정상적으로 접수되었음에도 `미체결 내역 조회` API가 빈 리스트를 반환하는 현상이 있습니다.
*   **대응책**: 본 시스템은 이를 해결하기 위해 **로컬 주문 상태 추적** 기능을 탑재하고 있습니다. API 조회 결과에 없더라도 로컬에 기록된 미체결 주문이 있다면, 설정된 시간 경과 후 자동으로 **강제 취소(Blind Cancel)** 및 **잔고 확인**을 수행하여 상태를 동기화합니다.

## � 프로젝트 구조 (Project Structure)

```text
my-stock-hts/
├── run.sh                # [Mac/Linux] 실행 스크립트
├── run.bat               # [Windows] 실행 스크립트
├── config.py             # 설정, 환경변수, 데이터 로드
├── api.py                # KIS API 통신 및 데이터 조회
├── constants.py          # 상수 정의 (TR ID, 필드 매핑 등)
├── indicators.py         # 보조지표 계산 (RSI, ADX, MACD 등)
├── utils.py              # 공통 유틸리티 (날짜, 포맷팅 등)
├── session.py            # 세션 및 토큰 관리
├── context.py            # 스레드 전역 상태 및 락(Lock) 관리
├── main.py               # 메인 실행 파일 (메뉴 및 라우팅)
├── LICENSE.md            # 라이선스 파일
├── db/                   # [자동 생성] SQLite DB 파일 저장소
├── json/                 # [자동 생성] 동적 설정 및 상태/캐시 파일 저장소
│   ├── stock.json              # 관심/감시 종목 리스트
│   ├── restricted_stocks.json  # 트레이딩 제한 종목 리스트
│   ├── daily_asset_state.json  # 당일 최초 시작 자산 기록 (일일 손실 제한용)
│   └── dynamic_config.json     # 프로그램 실행 중 변경된 시스템 설정 백업
├── logs/                 # [자동 생성] 로그 파일 저장소
├── chart/                # [자동 생성] 차트 이미지 저장소
├── data/                 # [자동 생성] 엑셀/CSV 내보내기 저장소
├── tools/                # 각종 진단 및 유틸리티 도구 (DB 초기화, 스레드 성능 시뮬레이션 등)
├── tests/                # Pytest 단위/통합 테스트 코드
└── modules/              # 기능별 모듈 폴더
    ├── db_manager.py     # DB 연결 및 쿼리 관리
    ├── db_queue.py       # SQLite 동시성 제어를 위한 싱글 워커 큐 프록시
    ├── telegram_bot.py   # 텔레그램 봇 연동 및 알림
    ├── settings.py       # [0] 시스템 설정 관리
    ├── market.py         # [1] 시장 지수 조회
    ├── analysis.py       # [2] 종목 시세 및 기술적 분석
    ├── chart.py          # [3] 차트 시각화 및 분석
    ├── trading.py        # [4] 종목 주문 관리 (매수/매도/정정/취소)
    ├── auto_trade.py     # [5] 시스템 트레이딩 (자동매매)
    ├── backtest.py       # [6] 전략 백테스팅
    ├── theme_analysis.py # [7] 테마 트랜드 분석
    ├── manage.py         # [8] 관심 종목 관리
    └── account.py        # [9] 자산 및 잔고 관리
```

## � 라이선스

이 프로젝트는 Apache License 2.0 하에 배포됩니다.